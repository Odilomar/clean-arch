{
  "name": "clean-arch-example",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "git@github.com:Odilomar/clean-arch.git",
  "author": "Odilomar <odilomarjunior.contact@gmail.com>",
  "license": "MIT",
  "scripts": {
    "prestart": "yarn migration:run",
    "start": "nodemon src/index.js",
    "migration:run": "npx sequelize db:migrate",
    "migration:create": "npx sequelize migration:create",
    "docker": "docker-compose up -d",
    "docker:drop": "docker rm clean_arch_db -f",
    "docker:reload": "yarn docker:drop && yarn docker"
  },
  "dependencies": {
    "date-fns": "^2.23.0",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "pg": "^8.7.1",
    "sequelize": "^6.6.5"
  },
  "devDependencies": {
    "nodemon": "^2.0.12",
    "sequelize-cli": "^6.2.0"
  }
}
